# SECflow configuration — see https://github.com/imsankz/SECflow
# Generated by secflow init — adjust engines/failOn/excludePaths for this repo.
engines:
  gitleaks: true
  trivy: false
  npmAudit: true
  regex: true
failOn: critical, high
excludePaths: node_modules, .git, dist, build, .next, vendor, package-lock.json, .worktrees, skills/seo-google/references
